# Mode B 生产架构契约

**本文件是 Mode B 唯一的生产架构依据。** 它解决的是“系统怎样自动交付一个可审计的配音项目”，不是某个节点、某个模型或某条测试数据能否运行。旧的端点说明、临时手工流程和与本文件冲突的设计描述均不作为生产行为依据。

当前实现已经有分离、聚类、翻译、TTS、时长适配和打包的组件；在下列契约落实前，它们是可调用的阶段工具，尚不是一条可自主交付的生产流水线。

## 1. 目标与唯一主线

```
上传源视频 / 源音频 + 字幕
  → separate-vocals
  → diarize
  → bind-speakers
  → translate
  → tts-generate
  → fit-and-qc
  → package
```

用户只启动一次 Mode B run。控制面负责继续推进这条链，前端只展示状态、问题和人工决策；前端、节点或人工调用的单独端点不能绕过阶段门槛并生成生产交付物。

## 2. 职责边界

| 部件 | 负责 | 不负责 |
| --- | --- | --- |
| 控制面 | 状态机、依赖、调度、产物登记、质量决策、审计 | 把计算过程或节点本地文件当真相 |
| Mode B 协调器 | 根据当前事实创建下一项幂等动作、补偿卡住的动作 | 保存第二套工作流状态 |
| GPU 节点 | 领取、计算、心跳、上传产物并报告能力 | 决定项目是否可交付 |
| 产物存储 | 保存可跨机器消费的输入与输出 | 将二进制写入数据库 JSON |
| 前端 | 启动、呈现、收集人工绑定/审核决定 | 编排后续阶段 |
| NodeJob | 节点健康探针 | 下发代码、计划任务或任意 shell 命令 |

`PipelineTask` 是阶段状态、依赖和重试的唯一真相。项目配置只能保存用户选择和小型运行元数据，不能保存阶段完成状态、文件路径或可被下游信任的输出。

## 3. 产物优先的完成语义

一次“任务完成”必须同时满足：

1. 计算已结束；
2. 此阶段要求的所有产物已经上传并通过大小、类型、摘要校验；
3. 此阶段的质量规则产生明确的 `pass`、`hold` 或 `fail`。

对产物型任务，节点提交计算结果后状态为 `awaiting_artifact`；只有产物登记和验证完成后才转为 `completed`。依赖解析器、协调器和重试器只把 `completed` 当成可解锁状态。这样“节点说做完了、文件尚未到达”不会推进任何下游阶段。

每个跨阶段产物必须有稳定的描述：

```json
{
  "key": "vocals",
  "producer_task_id": "task-id",
  "storage": "controlplane-local",
  "href": "/api/nodes/tasks/task-id/artifacts/vocals/vocals.wav",
  "filename": "vocals.wav",
  "bytes": 1234,
  "sha256": "…",
  "mime_type": "audio/wav"
}
```

`href` 是控制面持有的鉴权下载地址。节点可以缓存下载内容，但 `E:\\…`、`C:\\…`、`/tmp/…` 等本地路径不能出现在任务 payload、项目状态或另一个节点的输入契约中。后续切换 R2 只替换 `storage/href` 的实现，不改变调用方。

## 4. 协调器与状态推进

`mode_b_controller` 是一个无状态、可反复执行的协调器。每次运行只读取 `PipelineTask`、已登记 artifact、质量结论和人工决定，然后创建**唯一**的下一步；动作以稳定的 `task_key + input_hash` 去重，重复调用不能重复排任务或覆盖成功结果。

它由以下事件触发：

| 触发 | 协调器行为 |
| --- | --- |
| 创建/启动 Mode B run | 生成首个缺失阶段 |
| 任务完成、失败或超时 | 计算可执行的下一动作或转入 hold |
| artifact 上传/校验完成 | 解除 `awaiting_artifact`，再评估依赖 |
| 人工角色绑定/审核 | 校验决定并继续相应分支 |
| reaper 与定期 reconcile | 修复因事件丢失、节点离线或进程重启造成的停滞 |

状态推进规则：

| 已验证事实 | 可创建的下一步 | 阻断条件 |
| --- | --- | --- |
| 源文件、字幕完整 | `separate-vocals` | 输入缺失 |
| `vocals` artifact 已登记 | `diarize` | 分离失败或 artifact 不完整 |
| 聚类 JSON 与参考候选可用 | `bind-speakers` | 聚类质量 hold |
| 所有可配音角色已绑定或明确跳过 | `translate` | 人工绑定未决 |
| 翻译与可读性检查通过 | `tts-generate` | 译文 hold/fail |
| 每条真实 TTS 音频已登记 | `fit-and-qc` | 缺音频、占位音频、模型/参考信息缺失 |
| 所有必需片段 fit/QC 通过 | `package` | 任一片段 hold/fail、时长超窗或缺失 |

直接调用阶段端点只保留给恢复和管理员诊断；它们必须走同一输入校验、artifact 语义和质量门，不能直接生产 `package`。

## 5. 质量与可追溯性

每一阶段输出必须带有 `pass/hold/fail`，并记录输入 artifact SHA-256、生产 task、节点发布版本、stage/模型版本、提示词或翻译版本、参考音 artifact SHA-256 和时间。重跑永远产生新尝试，不覆盖已批准产物；协调器根据当前批准尝试选择下游输入。

| 阶段 | 自动门 | 人工/后续门 |
| --- | --- | --- |
| 分离 | 音频可读、时长与源对应、artifact 完整 | 听感抽检 |
| diarize | 聚类结构、参考片段可用、置信度阈值 | 角色归属确认 |
| 翻译 | 语义/禁词/可读性与时长预算 | 叙事、口语化、文化适配 |
| TTS | 非占位音频、可读、时长窗口、参考和模型可追溯 | 音色一致、情绪、发音 |
| fit | `[0.8, 1.2]` 时长比例或明确豁免 | 节奏与画面对齐 |
| package | 必需片段全数通过、清单完整、版本可复现 | 抽样听看与主观质量签核 |

技术 QC 通过只表示流水线可以安全推进，不等于成片具有高质量。后续应以真实剧集的盲听、角色一致性、字幕语义和返工率建立质量基线；这属于产品质量验证，不应混入工作流正确性判断。

## 6. 节点与发布

节点启动上报发布版本、已安装 stage、模型版本/缓存校验和可用显存；调度器只匹配满足任务能力的节点。模型缺失必须报告 `model_unavailable`，不消耗运行重试。

节点更新使用不可变版本包和 manifest：下载、摘要校验、切换、常驻重启、probe。实机补丁必须回收进仓库提交；节点本地不能长期成为“比仓库多一套能力”的隐性分支。

SQLite 在单控制面、单节点阶段可以继续使用。接入多节点长期生产或多进程控制面前迁移 Postgres；该迁移不改变上面的任务、artifact 和协调器契约。

## 7. 实施顺序与验收

1. **统一 artifact 生命周期。** 对产物型任务实现 `awaiting_artifact → completed`，补足 `href/SHA-256/MIME/producer_task_id`，并禁止本地路径跨机传递。
2. **接入协调器。** 让 run、task 终态、artifact 上传、人工决定和 reaper 都调用同一个幂等推进函数；前端移除后续阶段编排职责。
3. **封住交付出口。** `package` 必须验证整项目所需片段与 QC 门，任何缺失或 hold 都返回可行动的阻断原因。
4. **统一全部输入。** 源音、参考音和节点生成物都使用 artifact 接口，移除遗留注册表/控制面本地绝对路径作为任务输入的约定。
5. **真实质量基线。** 在可控小样上做端到端评审并记录可重复指标，再逐步扩大到生产剧集。

在第 1–3 项完成之前，Mode B 的正确定位是“已集成的阶段组件”，不能对外宣称为无人值守的生产流水线。
