# dubbing-system 云端部署方案（不依赖本地，全员浏览器可用）

> 2026-08-28。基于 V3-FINAL.md 定稿，回应三个要求：
> ①技术方案+整改流程 ②每步标注 CPU/GPU/需要用户准备什么 ③全云端租用，公司所有电脑打开浏览器就能用。

---

## 一、最终架构（一台 VPS + 按需租的 GPU + R2）

```
公司所有电脑（浏览器）
   │  HTTPS
   ▼
┌─ 腾讯轻量 VPS 2核4G（新开，~¥60/月）──────────────┐
│  Caddy :443  dubbing.opspilot.me + 账号密码        │
│  controlplane 容器  ← 全部CPU/IO任务在这里跑       │
│  Postgres 容器（任务队列=真理之源）                 │
└──────────────┬───────────────────────────────────┘
               │ 预签名URL（控制面只发凭证，不搬视频字节）
               ▼
┌─ Cloudflare R2 对象存储（视频/音频/成片仓库）──────┐
│  存储费≈¥11/月·百GB级  流出流量永久免费             │
└──────────────┬───────────────────────────────────┘
               ▲ 节点拉切片/传产物
┌─ compshare 4090 按量实例（用完关机，¥1.94/h）──────┐
│  gpunode容器 join进池：Demucs/FunASR/CosyVoice2    │
│  自带16核CPU+60G内存（实例费已含，不用另买CPU）      │
└───────────────────────────────────────────────────┘
```

**关键点**：
- 视频文件永远走「浏览器→R2」「R2→浏览器」直传直下，1核小水管 VPS 只发签名，带宽零压力
- CPU 任务（翻译/字幕/混音/编码/QC）全部在 VPS 的 2 核上跑，95% 的时间 GPU 实例是关机的
- 公司任何电脑打开 `https://dubbing.opspilot.me`，登录即用，本地零安装

---

## 二、每一步的资源需求表（你要准备什么）

### A. 你要准备的东西（一次性，共4件）

| # | 事项 | 花钱 | 时间 | 怎么做 |
|---|------|------|------|--------|
| A1 | **腾讯轻量 VPS 2核4G**（推荐）或升配现有43.134那台到2核4G | ¥60/月 | 10分钟 | 腾讯云控制台买，Ubuntu 24.04，把 SSH 密码/密钥发我（或临时建个账号） |
| A2 | **DNS 解析**：`dubbing.opspilot.me` → VPS IP | ¥0 | 5分钟 | 你在域名管理后台加一条 A 记录（我给你具体值） |
| A3 | **翻译 API key**（网页上填，不用发我）：DeepSeek / 智谱 / 任一 OpenAI 兼容 | 按量 ≈¥1-3/部 | 5分钟 | 平台网页 → Providers → 填 key → 点「测试连通」 |
| A4 | **R2**：已有账号，创建 API Token（S3 凭证三件套） | 已含 | 5分钟 | Cloudflare 控制台 R2 → Manage API Tokens → 生成后填网页（或 .env） |

> 红线确认：A1 新开 VPS 和 A2 加 DNS 都属于动线上资源，**你确认我才执行**，我全程远程操作，你只需要在网页上点。

### B. 流水线每一步的算力归属

| 步骤 | 任务 | 跑在哪 | 需要GPU? | 需要你准备 |
|------|------|--------|----------|-----------|
| 0 上传母片 | 浏览器→R2直传 | 你公司电脑 | ❌ | 无 |
| 1 探针/分片规划 | T010/T030/T060 | VPS CPU | ❌ | 无 |
| 2 说话人分离 | T040 diarize | **GPU实例** | ✅ pyannote | 开GPU实例 |
| 3 角色库 | T050 | VPS CPU | ❌ | 无 |
| 4 擦字幕(fast) | T110 | VPS CPU | ❌（quality模式才要GPU） | 无 |
| 5 人声分离 | T120 | **GPU实例** | ✅ Demucs | — |
| 6 识别 ASR+OCR | T130/T140/T150 | GPU+VPS各半 | ✅ FunASR | — |
| 7 五步翻译 | T205-T220 | VPS CPU | ❌ | A3的key |
| 8 TTS配音 | T310 | **GPU实例** | ✅ CosyVoice2 | — |
| 9 时长/混音/编码 | T320-T340 | VPS CPU | ❌ | 无 |
| 10 缝合+烧字幕+QC | T410-T430 | VPS CPU | ❌ | 无 |
| 11 成片下载 | R2→浏览器直下 | 你公司电脑 | ❌ | 无 |

**结论**：GPU 只在 2/5/6/8 四步用到，一部 90 分钟剧 GPU 连续占用约 2.4 小时 ≈ ¥4.7。其余全部在 ¥60/月的 VPS 上完成。

### C. 什么时候开 GPU（操作流程）

1. 网页上传完母片+SRT（或点「自动识别」）→ 翻译链立刻自动跑（VPS 上，不需要 GPU）
2. 网页出现「需要GPU」提示时 → 打开 compshare 实例（一条命令 join，或我帮你预配成开机自启）
3. GPU 跑完 T040/T120/T130/T310 → 网页提示可关机 → 关掉，停止计费
4. 断点续传保证：中途关机任务自动回队列，下次开机接着算，钱不白花

---

## 三、整改流程（现有系统 → 云端可用，分5步）

| 步 | 内容 | 改什么 | 验收 | 前置 |
|----|------|--------|------|------|
| W1 | **VPS 就位**：装 docker+compose，rsync 部署 controlplane（compose/Caddyfile 已写好），挂 R2 凭证 | deploy/ | `https://dubbing.opspilot.me/health` 返回 ok | A1+A2 |
| W2 | **Postgres 切换**：DATABASE_URL 换 PG 连接串，alembic 迁一次表 | 无代码改动（SQLAlchemy 双方言已就绪） | 网页建项目→seed→run-translate 全链在 VPS 上跑通 | W1 |
| W3 | **现有 Mac 开发数据收尾**：dev.db 冻结为开发库，面板容器化 | deploy/ | Mac 上 `docker compose up` 与 VPS 行为一致 | — |
| W4 | **HTTPS+账号**：Caddy basicauth（公司共用账号密码）+ API_TOKEN | deploy/Caddyfile | 外网访问强制登录，密码错误进不去 | W1 |
| W5 | **GPU 镜像预制**：gpunode Dockerfile（CUDA12.1+全部模型权重层）推镜像仓库；compshare 开机=拉镜像+join | gpunode/ | 4090 实例从开机到领任务 <10分钟 | 租一次卡做联调 |

每步我都能远程完成，需要你的只有：A1 买 VPS 给我 SSH、A2 点一下 DNS、A3/A4 网页填 key。

---

## 四、成本总账（月度）

| 项 | 金额 | 说明 |
|----|------|------|
| VPS 2核4G | ¥60 | 固定，全公司入口 |
| R2 存储 | ¥10-15 | 100GB 级，流出免费 |
| 域名 DNS | ¥0 | 已有域名 |
| GPU 按需 | ≈¥5/部 | 2.4h×¥1.94，用完关机不产生费用 |
| 翻译 API | ≈¥1-3/部 | 按量 |
| **固定月成本** | **≈¥75** | 按量部分产多少部花多少 |
| （对照：鬼手剪辑） | ¥17-151/部 | 同样工作量贵 3-30 倍 |

---

## 五、多语言与质量（已定稿，含在本方案内）

- TTS 按语种路由：en=CosyVoice2（GPU）/ id、es、pt 等=Confucius4 在线 API（**零 GPU，租卡前就能出这些语种**）/ 旁白=Kokoro（CPU）
- 翻译=五步链（术语表+前文连贯+角色人设+跨批终检+音节校验），对标并超越鬼手经典档
- 成片规范：-16 LUFS 响度、ASS 字幕烧录在原字幕位、QC 三查自动报告
- lipsync（口型同步）V1 不做：短剧观众对跨语种口型无预期，二期满需求再加（DAG 预留 T315 槽位）

---

## 六、需要你现在的决定（只这一个）

1. **VPS 选哪种**：方案①新开腾讯轻量 2核4G（¥60/月，干净隔离，推荐）／方案②现有 43.134 那台升配到 2核4G（省一台钱，但要停机升配+与现有业务共担风险）
2. 选好后买好，把 SSH 信息给我（新开的话），DNS 记录值我算好发你，你贴进域名后台

其余全部我来。GPU 联调（W5）可以等 W2 完成后随时约一次 compshare 开机实测。
