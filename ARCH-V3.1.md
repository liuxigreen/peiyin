# dubbing-system 技术架构方案 V3.1（多Agent协作 + GPU自动开关 + 全环节质量细节定稿）

> 2026-08-28。回应四问：①GPU干活比CPU快吗（逐环节算账）②4090自带CPU够用吗 ③字幕烧录细节（位置/行数/字号/颜色/透明度）④GPU自动开关+每步质量检查。
> 本方案为最终施工依据，V3-FINAL.md §7 与本文冲突处以本文为准。**先出方案，确认后实施。**

---

## 一、CPU vs GPU 逐环节算账（回应"是不是太慢了"）

原则：**不是GPU一定快——只有"视频像素级处理"和"深度模型推理"GPU才有数量级优势**；文件搬运、API调用、简单编码，GPU帮不上忙还多花钱（开机费）。

| 环节 | 数据规模 | CPU耗时(2核VPS) | GPU耗时(4090) | 定案 |
|------|---------|----------------|---------------|------|
| 五步翻译 | 纯API网络调用 | ~8min（受API速度限制，GPU无用） | 同样8min | **CPU(VPS)** |
| 字幕擦除fast(羽化) | 全片像素级模糊 | 90min片~40-60min ⚠️慢 | ~8min | **→GPU**（升级定案） |
| 字幕擦除quality(ProPainter) | 视频修复推理 | CPU不可行(>10h) | ~25min | **GPU** |
| Demucs人声分离 | 深度模型 | 不可行 | 18min | **GPU** |
| ASR(FunASR) | 深度模型 | 不可行 | 15min | **GPU** |
| TTS(CosyVoice2) | 深度模型 | 不可行 | 55min | **GPU** |
| 时长匹配/预拼混音 | 音频样本运算 | ~5min | ~1min(不值开机) | **CPU(VPS)** |
| **编码(encode/stitch)** | H.264像素级 | ~35min ⚠️ | **~6min(NVENC)** | **→GPU(NVENC)**（升级定案） |
| **字幕烧录(ASS)** | libass渲染像素级 | ~15min | **~3min(NVENC)** | **→GPU(NVENC)**（升级定案） |
| QC响度/静音分析 | 音频流过一遍 | ~2min | 同样 | **CPU** |
| 下载/上传/签名 | 网络带宽 | 取决于带宽 | 同样 | **CPU** |

**升级定案（相对V3-FINAL §7.1）**：T110擦字幕fast、T330/T340混音编码、T420烧录 → 从VPS CPU改到**GPU节点NVENC硬编码+像素处理**。理由：这三类是像素级运算，4090比2核VPS快5-10倍；且GPU反正要为TTS开机，顺路做边际成本≈0。

**改后的GPU占用账（90min剧）**：probe顺路+擦字幕8+分离18+ASR15+TTS55+混码烧录10 ≈ **连续2.2h ≈ ¥4.3/部**，VPS只干轻活。

## 二、4090自带CPU够用吗——够，但有前提

compshare 4090实例 = Xeon 16核 + 60G内存级配置（时租¥1.94已含）。够用的前提是**分工正确**：

```
GPU实例上跑的进程：
  gpunode主循环(领任务/心跳)         ~0.5核
  Demucs/FunASR/CosyVoice2推理       GPU为主,CPU喂音频IO ~4核
  ffmpeg NVENC编码/烧录              ~2核（NVENC硬件编码CPU占用极低）
  合计峰值 ~8核 < 16核 ✅ 内存峰值~20G < 60G ✅
```
风险点与对策：①FFmpeg滤镜链(wipe/eq)吃CPU→滤镜参数控制在轻量档 ②Demucs分块读盘→流式preload ③实测首个项目时记录真实峰值，超80%再把擦字幕quality降级回VPS慢慢跑。

**多Agent协作视图（整个系统=一条自动化工厂流水线）**：

```
┌─控制面(常驻)────────────────────────────────────────┐
│ 🎬 调度Agent(orchestrator): 任务表扫描/READY解锁/缓存  │
│ 🗡 Reaper Agent: 心跳超时→回收→重派(自动容错)          │
│ 💰 算力Agent(新): 监控gpu任务队列长度→自动开关GPU实例   │
│ 🌐 Web/API: 全公司入口                                │
└──────────┬──────────────────────────────────────────┘
           │ 任务队列(Postgres)
   ┌───────┴────────┬─────────────────┐
   ▼                ▼                 ▼
[VPS CPU池]   [GPU节点池]      [外部API池]
翻译/字幕生成/  diarize/Demucs/   翻译LLM(Provider表)
混音/音符QC    FunASR/CosyVoice2/ Confucius4-TTS
              NVENC擦字/编码/烧录  (多语言零GPU)
```
每个「Agent」是独立进程/线程，靠Postgres任务表协作，任何一方崩溃自动恢复——这就是多agent协作的工程形态。

## 三、GPU自动开关（算力Agent设计）

**事实基础**：scnet.cn（国家超算互联网/算力互联）OpenAPI 有完整容器实例管理接口——创建/启动/停止/删除容器实例、批量执行脚本。compshare 若走此平台则API直接可用；若不可用则降级为「一键开机脚本」（网页按钮，10秒操作）。

```
💰 算力Agent逻辑（控制面内线程，每60s）：
  when gpu队列等待任务数≥2 持续5分钟 且 无在线GPU节点:
      → 调OpenAPI开机(或推送"该开机了"到网页+TG)
      → 开机脚本自动: 拉镜像→join进池→领任务
  when gpu队列空 且 所有GPU任务completed 持续10分钟:
      → 通知(网页+TG): "GPU工作完成,10分钟后自动关机"
      → 无取消→调OpenAPI关机（结算停止）
  安全阀: 单日GPU费上限(默认¥50,网页可调), 超了只通知不自动开机
```
配置项进 `projects.config` + 全局settings：`gpu_auto_on/off`, `daily_gpu_budget`。

## 四、字幕烧录细节定稿（逐项回应）

| 细节 | 定案 | 依据 |
|------|------|------|
| **位置** | 置于原字幕带正中（覆盖式），T140 OCR实测y坐标动态注入ASS，每切片自动校准 | 短剧字幕位固定；原位替换避免双字幕 |
| **行数** | 智能折行：≤20字符一行；20-42按语义切两行（逗号/空格优先切点）；>42字压句（配合音节校验，超限进人工） | YouTube竞品标准：最多2行，手机可读性 |
| **字号** | 帧高6%（1920竖屏=115px），`projects.config.subtitle_font_ratio` 可调5-7% | 竞品实测5-6%区间 |
| **颜色** | 白字#FFFFFF + 黑描边4px + 20%投影；`config.subtitle_style` 可选高对比黄(#FFE94A) | 竞品主流白底黑边；戏剧冲突强的剧用黄 |
| **透明度** | 文字100%不透明；描边85%；无背景板（擦除已清场）。原字幕擦除质量差时可选半透明底板(α=0.35) | 底板只做兜底，默认纯净 |
| **字体** | Noto Sans SC Bold（黑体系，多语种fallback链自动切换） | 免费商用+全语种覆盖 |
| **时间对齐** | ASS时间码=配音轨实际时长对齐后写入（字幕跟配音走），漂移>100ms自动校正 | 核心原则 |
| **标点** | 台词末尾句号省略、问叹号保留；引号统一「」→" "（海外平台习惯） | Netflix中文字幕规范 |

实现：`render.py write_ass()` 升级参数化（折行函数+样式注入+band_y动态），QC增加「字幕可读性抽查」（抽10句人工确认位/行/字号）。

## 五、每步质量检查完善（全环节QC矩阵）

| 环节 | 自动质检项 | 不过关的处理 |
|------|-----------|-------------|
| 上传 | ffprobe可解析/分辨率≥720/时长>20min | 拒收，标原因 |
| 探针T010 | 软硬字幕判定置信度、VFR检出 | HDR/VFR自动转码再进 |
| 说话人T040 | 说话人数∈[2,10]、每角色时长≥30s | 异常→网页人工确认页 |
| 擦字幕T110 | 抽帧OCR残留检测（擦后帧底部25%无高置信文字） | 残留→该切片切quality模式重跑 |
| 分离T120 | vocal/background能量比合理(1.5-40dB) | 异常→换htdemucs_ft全量模式 |
| 识别T130/T150 | 双模edit_distance>0.2的CONFLICT率<5% | 超标→该批标黄人工页 |
| 翻译T220 | 音节比100%≤1.15、术语命中率100%、空译=0 | 超→压缩重写→人工红标 |
| TTS T310 | UTMOS≥3.0、时长比∈[0.8,1.2] | 低分换seed×3→人工审 |
| 混音T330 | 无削波(peak<-0.1dBFS)、人声/伴奏比合理 | 削波自动降gain重混 |
| **编码T340** | 分辨率/码率/帧率与源一致、无花屏(抽帧hash) | 花屏重编码 |
| **成片T430** | -16LUFS±1.5、意外静音=0、音画偏移<100ms、**字幕抽查10句位/行/号正确** | 任一不过→出报告+定位任务自动重跑 |
| 交付T440 | R2对象sha256校验、下载链200 | 失败重传 |

每项质检=任务完成钩子里的纯函数，结果写进task.output_paths QC字段，网页项目页「质检」Tab全量可见。失败自动重跑一次→仍失败标红并TG通知。

## 六、实施切分（先方案确认，再动工）

| 步 | 内容 | 算力 | 验收 |
|----|------|------|------|
| P1 | 算力Agent+OpenAPI对接(scnet容器开关机)+安全阀 | VPS | 模拟gpu积压→API开实例→join→清空→自动关 |
| P2 | render.py升级：折行/样式/动态band_y + T340/T420接NVENC(真视频) | GPU联调一次 | 90min测试片烧录+编码走NVENC，QC全绿 |
| P3 | QC矩阵落库：12环节质检钩子+网页质检Tab | VPS | 每环节人为造失败→钩子捕获→自动重跑/标红 |
| P4 | W系列云端部署（CLOUD-DEPLOY.md，等你拍板VPS） | — | dubbing.opspilot.me 全公司可访问 |
| P5 | E2E实战：1部真剧全流程+成本实测校准 | 按需 | V1验收十条全过 |

依赖关系：P2/P3可立即开工（Mac联调）；P1需确认compshare的OpenAPI凭证（你去控制台看一眼有没有API密钥入口，截图给我）；P4等你VPS决定。

---

*本文与 V3-FINAL.md / 三件套构成完整施工文档。实测冲突→改档记日期。*
