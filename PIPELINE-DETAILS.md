# PIPELINE-DETAILS.md — 全流程细节规格书（每个环节的模型/参数/质量决策）

> 隶属 DESIGN.md v2。本文回答"每一环到底怎么做、怎么做好"。定稿后 GPU 才是最后一件事。
> 2026-08-27 定稿。实测与本档冲突 → 改档记日期。

---

## 磁盘规划（回应"系统盘都放什么"）

```
100G系统盘预算（4090实例，Ubuntu-nvidia 22.04）：
├── 系统+CUDA+Torch运行时        ~14G
├── 模型权重合计                 ~8G
│   ├── pyannote 3.1            0.2G   (说话人分离)
│   ├── FunASR Paraformer-zh    1.0G   (中文ASR)
│   ├── Demucs htdemucs_ft      0.4G   (音源分离)
│   ├── Resemble Enhance        0.5G   (可选降噪)
│   ├── CosyVoice2-0.5B         5.0G   (英语TTS主力)
│   ├── Kokoro-82M              0.3G   (旁白兜底,CPU)
│   └── UTMOS                   0.1G   (TTS质检)
├── pip/conda缓存+Docker层       ~10G
├── 工作区：当前处理的剧集中间产物   ~30G峰值(一部剧)
└── 剩余缓冲                     ~38G
结论：默认100G够用，一分钱磁盘费不用花。
权重完整备份放R2(~¥0.9/月)，实例释放后5分钟热恢复。
ProPainter(2GB,二期)启用时也仍在100G内。
```

---

## Phase 3 识别与对齐 —— 质量细节

### 双模识别的分工本质
- **ASR管"耳朵"**：听到的字。强项：口语、吞音、语气词。弱项：同音字错（帐/账、在/再）
- **OCR管"眼睛"**：字幕上印的字。强项：人名、书面词、专有名词。弱项：艺术字、色差背景
- 合并策略：edit_distance/max_len < 0.2 → 字符置信度加权合并；≥0.2 → CONFLICT标红人工
- 实测预期：纯ASR ~2%错 → 双模 <0.5%

### FunASR 关键配置
```python
model="paraformer-zh", vad_model="fsmn-vad", punc_model="ct-punc"
hotword="角色名 剧名 专有名词..."   # 从glossary表自动生成，识别率人名提升显著
chunk_size=60000                    # 毫秒! ≤60s硬约束（否则显存暴涨14G+，issue#728）
batch_size_s=300
```
流程顺序坑：**VAD切好的句级片段逐段喂**，每段≤60s；置信度取各段均值

### PaddleOCR 时序聚合（去抖动）
同一位置连续≥3帧同文本才采纳；跨帧字幕拼接按 bbox x重叠判断换行合并

---

## Phase 4 翻译 —— 质量核心（本地零模型，全在prompt工程）

### 四步流水线（VideoLingo蒸馏的4个skill，已存在本机）
```
1. drama-knowledge-extractor  全剧先提取: 术语表+主题摘要+人物关系
2. drama-chunk-translator     两轮翻译: 第一轮直译→第二轮带上下文反思意译
3. drama-localization-rewriter 目标市场润色(us/esLatam/...文化梗适配)
4. drama-consistency-checker  一致性终检: 角色名/术语前后统一
```
### Chunk策略（质量的决定性参数）
- 600字符/批（Netflix经验值），批间携带前文最后3句译文保持连贯
- 角色卡(role_card)随批注入：男主"冷峻强势"→用词短促有压迫感
- 爽点词典强制替换：「打脸」「逆袭」「赘婿」→ 经目标市场验证的高留存表达
- 并发10-16、429退避30/60/90s、chunk失败拆半重试、敏感拒答脱敏回填

### 音节约束（配音能对上口型的前提）
```
英文译文字节数上限 = 原文中文字数 × 1.15 × (en速率比)
超限三级处理: LLM压缩重写 → 规则裁剪(虚词优先删) → 标红人工
每句落库 syllable_ratio 字段，Web对照表橙色预警 >1.05，红色 >1.15
```

---

## Phase 5 TTS 配音 —— 质量细节

### CosyVoice2 zero-shot 三要素（决定克隆效果）
| 要素 | 规格 | 为什么 |
|------|------|--------|
| 参考音频 | 3~15s，情绪中性，SNR最高段 | 中性段的音色最稳定，激情段会把语气烧进所有输出 |
| 参考文本 | 与参考音频逐字对应的中文 | text-prompt对齐显著降低幻听 |
| 输出控制 | speed因子 + 读两次择优 | 同seed温度不稳定时重试 |

### 跨语种口音问题预案（中→英特有）
1. 参考音频改选「情绪平稳+语速适中」段
2. RVC后处理把timbre拉回原声（二期）
3. 实在不行 GPT-SoVITS 微调该角色（40min/角色，5分钟干净语音即可）

### 时长匹配三级火箭
```
ratio ∈ [0.9,1.1]  直接过
ratio ∈ [0.8,1.2]  WSOLA变速不变调(librosa.effects.time_stretch)
其他              TTS prosody_rate 参数重新生成一轮 → 仍超 → 标红人工
```
### 质检闭环
UTMOS自动打分 <3.0 → 换seed重生×3 → 仍低 → 推入人工审列表
（不打分就上线 = 把跑调内容直接推给C端）

---

## Phase 6/7 混音缝合 —— 细节即产品质感

- 混音Python预拼（防open files），台词区间伴奏压-10dB，SFX时刻只压-4dB保冲击感
- loudnorm -16LUFS / -1dBTP（YouTube标准响度）
- 字幕ASS置于原字幕带中心替代原位，字号帧高5~6%，白字黑描边
- QC三查：全片LUFS方差<1.5；意外静音>3s数=0；每5min抽10句音画偏移<100ms

---

## 4090 实例吞吐账（回复"多久出片多少钱"）

| 阶段 | 耗时 |
|------|------|
| 预分析(pyannote/VAD/分片) | 12min |
| 擦字幕(fast羽化) | 8min |
| Demucs分离×30切片 | 18min |
| ASR+OCR双模 | 15min |
| 翻译(API并行不占GPU) | 8min |
| **TTS 2000句@batch4** | **55min(GPU大头)** |
| 混音+缝合+编码 | 25min |
| **合计** | **≈2.4h ≈ ¥4.7/部** |

## 部署形态定稿（GPU是最后一步）

1️⃣ 现在：Mac当临时控制面+R2真桶已通 → **先把4个翻译skill接进控制面跑通Phase4纯翻译链路（零GPU成本）**
2️⃣ 然后：租4090华北一C(¥1.94/h) → gpunode join → GPU链路逐阶段点亮
3️⃣ 最后：全流程E2E实测校准耗时/成本表

*GPU不是不重要，是它最后10%才登场——前90%的确定性现在就能拿到。*
