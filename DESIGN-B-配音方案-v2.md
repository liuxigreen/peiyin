# 模式 B 配音方案 v2：谁在说话 → 用什么声音（完整链路设计）

> 2026-09-01。目标：B方案（字幕+原配音音频）交付的每一句配音，
> 明确"这句话该用哪个角色的哪种声音念"，全程自动化、可追溯、可人工修正。
> 借鉴鬼手剪辑多角色配音链路（语音/文本/声纹多模态识别 + 角色独立克隆 + 跨场景音色锁定），
> 结合我们已有资产（C0角色提取、翻译链、节点协议）。

## 一、核心问题（之前没设计清楚的部分）

B方案我们有的：SRT（每句文本+时间窗）、原配音音频（按时间窗切成每句参考音）、
角色档案（38角色+性别/年龄/音色描述）、英文译文。
缺的：**每句参考音 对应哪个角色 这件事没有数据**——speaker_id 字段全空。
没有这一步，"用哪个音色"就只能随机，多角色剧直接露馅。

## 二、解决方案：音频聚类 → LLM 绑定（两步，全自动）

```
Step 1 声纹聚类（pyannote / 3060节点）
  输入：全部 2803 句参考音切片（音频已按 SRT 时间窗切好）
  输出：每句 → 声纹簇 ID（如 C01..C12），同簇=同一个人声
  精度：短剧配音演员少（通常 3-8 人），聚类效果比"连续对话中分离"好得多
  参考：鬼手"语音/声纹多模态识别"第一步即此

Step 2 LLM 角色绑定（用现有 C0 角色提取，纯 API）
  给 LLM：全部声纹簇 + 每簇抽样台词文本 + C0 提取的角色档案
  让它做三件事：
  a) 每簇 → 角色（"C01 的声音说'楚弋你不许走'→ 楚弋")
  b) 每簇 → 音色策略：
     · 主角 → 克隆该簇参考音（CosyVoice 3 跨语种，中文原声→英文配音）
     · 配角 → 克隆或音色库
     · 龙套 → 音色库按性别/年龄匹配
  c) 每句 → 情绪标签（angry/whisper/…供 TTS instruct，可选）
  结果落库：utterances.speaker_id 关联 speakers；角色表存参考音频簇

修正入口：Web 端角色管理页可改绑定/换音色 → 单句重跑（节点协议已支持）
```

## 三、B 方案 TTS 生产链路（3060 节点）

```
对每一句（按角色分组批量）：
  text=英文译文（翻译链产物） + lang=en
  ref_audio=该角色声纹簇里 SNR 最高的一两句切片（Demucs 提纯后）
  engine=cosyvoice_api（3060 上 CosyVoice 3 服务）
  → wav（时长自检：>窗口×1.15 → WSOLA 拉伸或回译压缩）
  → 按原窗口落位 → 配音轨

多角色并发：同一角色的连续句合并 batch（CosyVoice 支持），显存友好
```

## 四、配套（逐步落地）

1. 角色管理 API：speakers 表已支持（ref_audio_pool/音色元数据），补绑定接口
2. tts_clips 表：产出逐句 wav + 引擎/时长/UTMOS 字段（模型已有）
3. 混音：Demucs 伴奏 + 配音轨（Ducking）→ 成片（B方案交付包升级）

## 五、与鬼手的差距（诚实版）
- 鬼手"跨集/整部连续识别"= 我们的全局声纹聚类（同款思路）
- 鬼手"上下文多句合并生成再切分"= 我们暂用逐句 TTS，流畅度略逊，二期可做句组批量
- 鬼手"表演级情感复刻"= 我们靠参考音情绪继承，CosyVoice 3 instruct 可加，二期
- 我们领先：翻译韧性链（审查/兜底/压缩）、角色提取、成本透明（全本地）

## 六、接入 3060 后的验证路径
1. 节点跑 `tts-generate` mock → 协议闭环 ✅（已完成代码）
2. 白月光 2803 句参考音 → pyannote 聚类（节点 `diarize` stage，待补）
3. LLM 绑定角色 → 抽样核对
4. CosyVoice 3 出 5 句试听 → 音色/时长验收 → 批量
